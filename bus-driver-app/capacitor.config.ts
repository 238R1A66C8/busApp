import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rtctracking.driverapp',
  appName: 'RTC Driver App',
  webDir: 'build'
};

export default config;
